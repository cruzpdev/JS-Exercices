Responsive site design for web and mobile using:
HTML
CCSS (Flexbox)
JavaScript

CHAR SELECTION BUTTON: 
1.get the buttons in JS to be able to check when the user clicks on one of them
2.add "selected" class to clicked button
3.check if button is already selected. if so, remove the selection from it